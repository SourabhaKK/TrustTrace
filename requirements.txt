datasets>=3.0
pandas>=2.0
pydantic>=2.0
scikit-learn>=1.3
numpy>=1.26
pyarrow>=15.0

# Serving + observability (Phase 2)
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
opentelemetry-sdk>=1.44
opentelemetry-exporter-otlp>=1.44
opentelemetry-instrumentation-fastapi>=0.65b0

# Fine-tuning (Phase 3) — QLoRA. torch must be a CUDA build (e.g. torch==2.5.1+cu121);
# install it separately per your CUDA version. bitsandbytes needs an NVIDIA GPU (cc >= 7.5).
transformers>=4.45
peft>=0.13
bitsandbytes>=0.44
accelerate>=1.0
mlflow>=2.16
